import { HomeHeaderNavClient } from "@/components/home-header-nav-client";

/**
 * Marketing home header — guest links only; scroll + mobile menu live in client.
 */
export function HomeHeaderNav() {
  return <HomeHeaderNavClient />;
}
